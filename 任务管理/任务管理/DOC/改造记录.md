# 改造记录

本文件用于记录 AG240101 刀片项目的所有协作改造记录。

## 记录规范

后续统一按“日期分组”的方式追加，格式如下：

### 2026-05-22

- 【改造模块/功能】：[模块名]
- 【改造清单】：[详细内容]

同一天有多项改造时，继续在同一个日期标题下追加新的模块和清单，不再重复创建新的日期字段。

## 2026-05-21

- 【改造模块/功能】：缓存任务调度与缓存列表展示
- 【改造清单】：1. 调整 `TaskCacheProcessor` 的候选任务筛选逻辑，优先任务会先进入处理队列；同巷道普通任务会被优先任务阻塞，不再跳过优先任务直接执行。2. 增加按巷道分组的判断兜底逻辑，优先使用 `ShelvesIdentification`，缺失时使用点位前两位数字推导巷道。3. 调整缓存任务列表查询排序，让 `Priority = 10` 的优先执行任务在服务端查询和页面展示中置顶。4. 补充改造记录文件初始化模板，后续协作按该文件持续追加记录。

- 【改造模块/功能】：缓存任务列表置顶规则修正
- 【改造清单】：1. 修正 `CachedTasks` 页面排序，仅将 `Status = 0` 且 `Priority = 10` 的待执行优先任务置顶。2. 修正缓存任务列表服务端查询排序，已取消任务不再因优先级被排到最前。3. 保持列表中待处理任务优先显示，避免取消任务影响置顶视觉顺序。

- 【改造模块/功能】：缓存任务页面文案修正
- 【改造清单】：1. 将 `CachedTasks` 页面列表列名从“最后错误”恢复为“缓存原因”。2. 将空白缓存原因的显示文案调整为“暂无缓存原因”，避免页面上看起来像缓存原因字段被去掉。

- 【改造模块/功能】：任务页面批量取消缓存任务
- 【改造清单】：1. 在 `Index.cshtml` 的缓存任务区增加批量取消按钮。2. 为待处理缓存任务增加勾选框，仅允许勾选 `Status = 0` 的任务。3. 新增 `BatchCancelCachedTasks` 接口与服务方法，批量将选中的缓存任务置为已取消。4. 批量取消沿用现有权限控制与刷新交互。

- 【改造模块/功能】：批量取消缓存任务日志补充
- 【改造清单】：1. 在 `TasksController.BatchCancelCachedTasks` 中增加操作人、角色、任务 ID 列表的请求日志。2. 增加批量取消结果日志，便于审计是否由人为操作触发。3. 在 `TaskService.CancelCachedTasks` 中增加开始/完成日志，记录目标数量与实际取消数量。

- 【改造模块/功能】：批量取消缓存任务勾选状态保留
- 【改造清单】：1. 将缓存任务勾选状态保存到前端 `sessionStorage`，页面刷新后自动恢复。2. 为单条取消和批量取消成功后同步清理已选缓存 ID，避免已取消任务继续保留勾选。3. 增加全选框与多选状态联动，防止刷新或局部操作后状态不一致。

- 【改造模块/功能】：缓存任务自动刷新勾选恢复
- 【改造清单】：1. 在 `refreshTaskData()` 的 AJAX 局部刷新成功后重新恢复缓存任务勾选状态。2. 将勾选状态恢复与全选状态更新抽成统一函数，避免自动刷新后仅恢复数据未恢复样式。3. 保持手动刷新和自动刷新表现一致。

- 【改造模块/功能】：缓存任务整页全选与操作框收口
- 【改造清单】：1. 在缓存任务表头增加“全选当前页缓存任务”复选框，实现当前页一键批量选择。2. 将顶部自动刷新、创建任务、导出 Excel、任务统计、普通/缓存筛选和批量取消按钮收纳到统一操作框内。3. 为普通任务和非待处理缓存任务补齐占位列，保持表格列宽与表头一致。4. 自动刷新后改为在下一帧恢复缓存勾选状态，减少局部重绘后样式未选中的问题。

- 【改造模块/功能】：缓存任务取消成功静默提示
- 【改造清单】：1. 移除单个取消缓存任务成功后的弹框提示，取消成功后直接刷新页面。2. 移除批量取消缓存任务成功后的结果弹框提示，批量成功后同样静默刷新。3. 保留失败和异常提示，避免成功操作打断连续处理节奏。

## 2026-05-22

- 【改造模块/功能】：缓存任务派发逻辑文档整理
- 【改造清单】：1. 新增 `DOC/TaskCacheProcessor派发逻辑说明.md`，整理 `TaskCacheProcessor` 当前版本的整体轮询、优先任务筛选、普通缓存任务排序、点位校验和重试保留逻辑。2. 明确记录普通缓存任务中的“相反类型/相同类型 + 同货架/不同货架”实际是排序权重，不是硬编码的四段式处理分支。

- 【改造模块/功能】：改造记录文档结构调整
- 【改造清单】：1. 将 `DOC/改造记录.md` 从平铺的单条记录格式整理为按日期分组的结构。2. 保留 2026-05-21 既有改造内容不丢失，便于后续同一天继续在同一日期标题下追加记录。3. 统一后续协作记录方式，减少日期重复和查阅成本。

- 【改造模块/功能】：缓存派发优化方案文档更新
- 【改造清单】：1. 重写 `DOC/TaskCacheProcessor派发逻辑说明.md`，聚焦扫描频率、0/1/2/3 分排序、每轮普通任务处理方式、同巷道判断 4 个优化点。2. 补充当前逻辑与优化方案的对比说明，明确日期排序在优化后保留为同分条件下的次级排序。3. 补充效率收益说明，明确缓存任务响应速度预计可提升约 10 倍以上。

- 【改造模块/功能】：缓存派发方案文档第 3 点修正
- 【改造清单】：1. 将 `DOC/TaskCacheProcessor派发逻辑说明.md` 中第 3 点从“每轮少量派发普通任务”的表述修正为“按空闲巷道逐条派发普通任务”。2. 明确同一个 `ShelvesIdentification` 在没有执行中任务时，每次只成功派发 1 条最优缓存任务。3. 明确若当前最优任务因点位锁定、拆分条件或业务校验失败，则继续尝试该巷道排序中的下一条任务。

- 【改造模块/功能】：终点空满限制分析后代码回退
- 【改造清单】：1. 回退 `Service/RcsTaskService.cs` 中基于 `TaskIdentification == 1` 放开终点空满状态限制的临时改动。2. 恢复 `AddTaskAsync` 和拆分任务类型 2 中原有的自动分配终点查询条件。3. 由于业务口径尚未确认“TaskIdentification = 1` 是否本就不应自动分配终点”，本次先恢复原状再继续分析。

- 【改造模块/功能】：按最终口径恢复入库任务终点空满限制放开
- 【改造清单】：1. 根据最终确认的业务口径：`fgHandover` 和 `shipment` 不会出现 `TaskIdentification = 1`，重新启用 `TaskIdentification == 1` 时放开终点空满状态限制的逻辑。2. 在 `Service/RcsTaskService.cs` 的 `AddTaskAsync` 自动分配终点查询中，入库任务只校验终点未锁定，不再校验 `Quanitity` 空满状态。3. 同步恢复拆分任务类型 2 中自动分配第二段终点的相同规则，保证 `TaskIdentification == 1` 的任务终点不受空满限制。

- 【改造模块/功能】：文档补充入库任务终点规则
- 【改造清单】：1. 在 `DOC/TaskCacheProcessor派发逻辑说明.md` 中新增“TaskIdentification = 1 的任务终点不看空满”小节。2. 明确记录 `TaskIdentification = 1` 表示入库/上架任务，终点仍需未锁定，但不再受 `Quanitity` 空满状态限制。3. 在优化前后对比和最终结论中同步补充该规则，便于后续统一理解。

- 【改造模块/功能】：TaskCacheProcessor 按四点方案改造
- 【改造清单】：1. 将 `TaskCacheProcessor` 的缓存扫描节奏从固定 1 分钟改为动态扫描：有待处理缓存任务时快速扫描，无任务时降频扫描。2. 将普通缓存任务候选选择改成显式 `0/1/2/3` 分规则，同分时只按 `CreateTime` 排序。3. 将普通缓存任务改成按 `ShelvesIdentification` 维度逐条派发：空闲巷道每次只成功派发 1 条最优任务。4. 将同巷道判断统一收敛为仅使用 `ShelvesIdentification`，移除原有点位前两位和中转组参与普通缓存调度筛选的逻辑。5. 同步将优先缓存任务也收敛为按空闲巷道选择，避免同巷道在一轮内连续放出多条任务。

- 【改造模块/功能】：TaskCacheProcessor 同分排序规则简化
- 【改造清单】：1. 将优先缓存任务和普通缓存任务的候选查询排序简化为只按 `CreateTime ASC`。2. 将按巷道选任务时的同分比较规则简化为只按 `CreateTime`，移除 `RetryCount` 和 `Id` 的次级排序影响。3. 同步更新 `DOC/TaskCacheProcessor派发逻辑说明.md`，确保方案文档与代码实际行为一致。

- 【改造模块/功能】：普通任务列表未完成未取消置顶
- 【改造清单】：1. 调整 `Services/Tasks/TaskService.cs` 中 `GetUserTasks` 的排序逻辑，在 `Tasks/Index` 普通任务列表里优先显示“未完成且未取消”的任务。2. 置顶判断条件统一为 `taskStatus < TaskFinish` 且 `IsCancelled = 0`，避免已取消中的任务继续排在前面。3. 保持原有字段排序能力不变，仅在所有排序前增加置顶层级。

- 【改造模块/功能】：自动分配终点缓存重试与缓存原因实时更新
- 【改造清单】：1. 修正 `TaskCacheProcessor` 对 `fgHandover`、`shipment` 缓存任务的预校验逻辑，重试前不再使用历史缓存终点做锁定拦截，避免自动分配终点任务被旧终点卡死无法重新下发。2. 调整 `RcsTaskService.CacheTaskAsync` 更新已存在缓存任务时的字段同步逻辑，除 `LastError` 和 `RetryCount` 外，同步刷新 `SourcePosition`、`TargetPosition`、`RequestCode`，保证缓存快照跟随本轮最新分配结果。3. 将缓存任务页面展示的缓存原因改为按最新失败原因实时覆盖更新，不再长期保留第一次进入缓存时的旧原因。

- 【改造模块/功能】：缓存任务列表表头排序
- 【改造清单】：1. 为 `Tasks/Index` 的缓存任务页补通 `sortColumn`、`sortDirection` 参数传递链路，使缓存任务和普通任务一样支持点击表头排序。2. 在 `TaskService.GetCachedTasks` 中增加缓存任务字段白名单映射，支持按任务类型、起始点、终点、状态、PO、物料、SU、类型、创建时间、确认时间、重试次数、优先级、缓存原因等列排序。3. 保留原有“待处理优先、优先执行任务置顶”的基础规则，再在同层级内按用户点击列排序。

## 2026-05-26

- 【改造模块/功能】：RCS 任务状态职责收口
- 【改造清单】：1. 调整 `Service/RcsTaskService.cs` 的 `UpdateTaskStatusAsync`，接口保留兼容响应和请求日志，但不再写入 `RCS_UserTasks.taskStatus`，避免任务管理系统按托盘号批量覆盖拆分任务状态。2. 调整 `CancelTaskAsync`，取消接口只写入 `IsCancelled = 1` 表达取消意图，不再由任务管理系统直接将未执行任务改为 `taskStatus = 30`。3. 明确 `RCS_UserTasks.taskStatus` 由调度主系统根据 `NdcTask_Moves`、NDC/ACI 实际执行链同步维护，任务管理仅负责接单、拆分、缓存和取消意图，避免第一段已完成但整组被误取消的问题。

## 2026-05-27

- 【改造模块/功能】：取消链路日志补充
- 【改造清单】：1. 在 `Controllers/RcsTaskController.cs` 的 `updateTaskStatus` 接口补充请求和返回日志，明确当前接口仅保留兼容响应。2. 在 `Service/RcsTaskService.cs` 的 `CancelTaskAsync` 中补充按托盘号查到的任务明细日志、拆分任务分组日志、每个分组的可取消任务清单日志，以及逐条写入 `IsCancelled` 取消意图的日志。3. 增加“分组跳过”日志，便于现场区分是任务已完成/已取消，还是当前请求真正写入了取消意图。

- 【改造模块/功能】：Tasks 页面取消任务交互收口
- 【改造清单】：1. 调整 `Views/Tasks/Index.cshtml` 中普通任务的 `cancelTask` 前端逻辑，取消成功后不再弹出成功提示框，改为直接刷新页面。2. 保持缓存任务和批量取消缓存任务现有行为一致，只有取消失败或请求异常时才弹框提示。3. 减少 Tasks 页面取消任务时的重复确认和额外点击，优化普通任务与缓存任务的交互体验。

- 【改造模块/功能】：拆分任务中转位提前放行
- 【改造清单】：1. 调整 `Service/TaskCacheProcessor.cs` 的巷道忙碌判断，将“拆分任务第二段已取货完成（`TaskSequence > 1` 且 `taskStatus = PickDown`）”从 busy lane 集合中剔除，避免继续阻塞同巷道缓存任务。2. 调整 `Service/RcsTaskService.cs` 的 `IsLocationOccupiedByActiveTaskAsync`，对拆分任务第二段起点中转位在 `PickDown` 后不再判定为执行中占用。3. 与主系统的中转位提前解锁逻辑配套，使任务管理在 AGV 取货成功后即可继续尝试下发复用该中转位的新任务，缩短整体节拍。

- 【改造模块/功能】：拆分任务提前放行日志补充
- 【改造清单】：1. 在 `Service/TaskCacheProcessor.cs` 的 busy lane 判断中补充日志，明确记录“拆分任务第二段到达 `PickDown` 后被主动排除出忙碌巷道”的任务号、起终点和巷道。2. 在 `Service/RcsTaskService.cs` 的 `IsLocationOccupiedByActiveTaskAsync` 中补充日志，明确记录“拆分任务第二段起点在 `PickDown` 后被占用判断主动放行”的点位和任务分组。3. 便于现场区分是提前放行生效，还是仍有其他正式任务/锁位条件在阻塞下发。

- 【改造模块/功能】：拆分任务提前放行条件放宽
- 【改造清单】：1. 将 `Service/TaskCacheProcessor.cs` 和 `Service/RcsTaskService.cs` 中“拆分任务第二段可提前放行”的判断条件，从仅 `taskStatus = PickDown` 放宽为 `taskStatus >= PickDown && taskStatus < TaskFinish`。2. 兼容现场任务状态直接跳过 `PickDown` 的场景，避免第二段已进入后续执行状态但仍继续阻塞巷道和中转位占用判断。3. 保持 `TaskFinish` 仍由完成态逻辑单独处理，不影响最终落库和完成通知。

## 2026-06-09

- 【改造模块/功能】：缓存任务同巷道普通任务补选逻辑
- 【改造清单】：1. 修改 `Service/TaskCacheProcessor.cs` 普通缓存任务选择逻辑，同巷道最高分普通任务因起点锁定、终点锁定或终点占用不可执行时，不再占用本轮巷道机会，而是继续尝试同巷道后续任务。2. 新增缓存任务点位可用性结果判断，保留 `fgHandover`、`shipment` 自动分配终点任务只预检起点的规则。3. 新增普通任务终点占用判断，并保持 `TaskIdentification == 1` 入库任务不受终点空满状态限制的既有业务口径。4. 对被补选跳过的缓存任务更新 `RetryCount` 和 `LastError`，便于后续排查不可执行原因。5. 补充同巷道补选方法和点位预检方法的文档注释，说明只绕过起点/终点类阻塞，不绕过优先级任务和中转位等待。

## 2026-06-12
- 【改造模块/功能】：shipment 自动分配终点重复锁定修复
- 【改造清单】：1. 修正 `Service/RcsTaskService.cs` 中 `fgHandover`、`shipment` 自动分配终点已在 `AddTaskAsync` 前置锁定后，后续普通校验再次因 `Lock = 1` 进入缓存的问题。2. 修正拆分任务类型 1/2 的终点抢锁逻辑，自动分配任务不再对已预锁终点重复执行 `TryLockLocationByNodeRemarkAsync`，避免缓存重试时反复报“终点已被锁定”。3. 增加对应日志，明确终点锁来自 `AddTaskAsync` 的预锁而不是外部占用。



## 2026-06-16
- 【改造模块/功能】：缓存任务退避调度算法防饿死与底层锁修复
- 【改造清单】：1. **修复底层锁定机制**：在 Service/RcsTaskService.cs 的 TryLockLocationByNodeRemarkAsync 中，针对 Dapper 将数据库 NULL 映射为 false 但实际 UPDATE 条件匹配不到 NULL 的问题，增加 ([Lock] = 0 OR [Lock] IS NULL) 的条件，彻底解决中转点被误报锁定导致死循环的问题。2. **引入退避算法防同巷道后车饿死**：修改 Service/TaskCacheProcessor.cs 中提取候选集的 SQL 排序，从纯粹的 ORDER BY CreateTime ASC 升级为带退避惩罚的 ORDER BY DATEADD(second, ISNULL(RetryCount, 0) * 30, CreateTime) ASC。同时将 SelectRegularTasksByLaneAsync 选最佳任务的内存排序变更为 .ThenBy(task => task.CreateTime.AddSeconds(task.RetryCount * 30))，确保死循环失败任务逐步让出调度名额。3. **完善全链路拦截日志体系**：在 Service/TaskCacheProcessor.cs 缓存因起点/终点不可用跳过时追加具体的 unavailableReason 原因打印。并在 Service/RcsTaskService.cs (AddTaskAsync) 的任务无效、起点缺失、终点缺失等 3 处“死局拦截点”补充 _logger.LogWarning 预警输出，防止死循环任务阻塞且无法排查。

## 2026-06-22
- 【改造模块/功能】：fgHandover 与 shipment 自动终点缓存专项通道
- 【改造清单】：1. 新增 `AutoDestinationAllocator` 公共自动终点分配服务，统一 `fgHandover -> fghandover`、`shipment -> dispatch` 的终点查询、空满校验和原子锁定逻辑。2. 在 `TaskCacheProcessor` 中新增 `fgHandover` / `shipment` 专项缓存处理通道，每轮最多处理 3 条，只预检起点并在缓存处理器内预分配终点，分配失败时直接更新缓存原因供页面展示。3. 将 `fgHandover` / `shipment` 从普通缓存候选 SQL 中排除，避免其继续受普通任务的 `ShelvesIdentification` 巷道分组和终点预检影响。4. 扩展 `AddTaskRequest.IsDestinationPreAllocated`，使 `AddTaskAsync` 能识别缓存处理器已预分配并锁定的终点，避免重复自动分配和重复终点锁拦截。5. 增加预分配终点失败释放逻辑和专项日志，若 `AddTaskAsync` 未生成正式任务则释放本次预分配终点并将失败原因写回缓存任务。

## 2026-06-23

- 【改造模块/功能】：任务拆分与起点锁定机制优化
- 【改造清单】：1. 优化了 `Service/RcsTaskService.cs` 中的 `SplitTaskType1Async` 和 `SplitTaskType2Async` 拆分任务生成逻辑，去除了对起点 `request.sourceBin` 执行 `TryLockLocationByNodeRemarkAsync` 锁定库位的操作，仅保留起点已标记锁定状态时的安全防呆检查与缓存机制。

## 2026-07-01

- 【改造模块/功能】：缓存任务下发与中转位锁定检测优化
- 【改造清单】：1. **优化中转点占用检测**：在 `Service/RcsTaskService.cs` 的 `IsTransitPointOccupiedByTaskAsync` 中，优化对中转位关联活动任务的 SQL 查询和 C# 过滤逻辑，将状态校验范围统一为 `taskStatus < TaskFinish` 且 `IsCancelled != true`。彻底解决已取消任务、完成态洗车等非标准结束任务导致中转点被误判永久占用的问题。2. **修复 API 取消的锁遗留问题**：在 `CancelTaskAsync` 接口中，针对未执行（`taskStatus == None`）的任务，增加解锁起点和终点储位（更新 `RCS_Locations` 表的 `[Lock] = 0`）的操作，使得通过接口取消的任务不会残留储位锁，从而保障同巷道和中转链条的后续任务能正常下发。
