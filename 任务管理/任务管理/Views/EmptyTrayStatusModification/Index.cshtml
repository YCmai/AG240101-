
<div class="row mb-3" style="margin-top:1%">

</div>

<div class="table-responsive">
    <table class="table table-striped table-bordered text-center">
        <thead class="table-light">
            <tr>
                <th>地图节点</th>
                <th>节点描述</th>
                <th>状态</th>
                <th>操作</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Items)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>@item.NodeRemark</td>
                    <td>
                        @if (!string.IsNullOrEmpty(item.MaterialCode))
                        {
                            <span style="color:red;font-weight:bold">Full</span>
                        }
                    </td>
                    <td class="text-left">

                       @*  <a href="javascript:void(0);" data-bs-toggle="modal" onclick="resetMaterialCode('@item.Id')" class="btn btn-sm btn-danger">ResetState</a> *@
                        <a href="javascript:void(0);" data-bs-toggle="modal" data-bs-target="#deleteModal" onclick="resetMaterialCode('@item.Id')" class="btn btn-sm btn-danger">重置状态</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<!-- 分页导航，右下角显示 -->
<div class="d-flex justify-content-end">
    <nav aria-label="Page navigation">
        <ul class="pagination pagination-sm">
             @if (Model.HasPreviousPage)
            { 
                <li class="page-item">
                    <a class="page-link" href="?page=@(Model.PageNumber - 1)&searchString=@ViewData["searchString"]">上一页</a>
                </li>
             } 

            <li class="page-item disabled">
                <span class="page-link">当前页面: @Model.PageNumber / @Model.TotalPages</span>
            </li>

            @if (Model.HasNextPage)
            { 
                <li class="page-item">
                    <a class="page-link" href="?page=@(Model.PageNumber + 1)&searchString=@ViewData["searchString"]">下一页</a>
                </li>
             } 
        </ul>
    </nav>
</div>
<!-- 消息模态框 -->
<div class="modal fade" id="messageModal" tabindex="-1" aria-labelledby="messageModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content shadow-lg border-0">
            <div class="modal-header bg-@(TempData["MessageType"]?.ToString().ToLower()) text-white">
                <h5 class="modal-title" id="messageModalLabel">
                    <i class="bi bi-check-circle me-2"></i>Prompt
                </h5>
                <!-- 修复关闭按钮的样式问题 -->
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close" style="filter: invert(1);"></button>
            </div>
            <div class="modal-body text-center p-4">
                <p class="lead">@TempData["Message"]</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="closebtn" data-bs-dismiss="modal">关闭</button>
            </div>
        </div>
    </div>
</div>




<!-- 删除确认模态框 -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalLabel">ConfirmDelete</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Are you sure you want to delete the storage location "<strong id="itemName"></strong>" 确认删除吗?。
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">取消</button>
                <button type="button" class="btn btn-danger" id="confirmDeleteButton">确认</button>
            </div>
        </div>
    </div>
</div>


<script>
    // function resetMaterialCode(id) {
    //     $.ajax({
    //         url: '@Url.Action("ResetMaterialCode", "EmptyTrayStatusModification")',  // 使用控制器和方法名称
    //         type: 'POST',
    //         data: {
    //             id: id,
    //             __RequestVerificationToken: $('input[name="__RequestVerificationToken"]').val()  // CSRF 验证
    //         },
    //         success: function (response) {
    //             // 显示操作结果消息
    //             $('#messageModal .modal-body').text(response.message);
    //             $('#messageModal .modal-header').removeClass('bg-success bg-danger')
    //                 .addClass(response.success ? 'bg-success' : 'bg-danger');
    //             $('#messageModal').modal('show');

    //             if (response.success) {
    //                 // 如果操作成功，更新表格显示
    //                 setTimeout(function () {
    //                     location.reload(); // 重新加载页面以更新状态
    //                 }, 3000);
    //             }
    //         },
    //         error: function () {
    //             $('#messageModal .modal-body').text("Reset failed, please try again.");
    //             $('#messageModal .modal-header').removeClass('bg-success bg-danger').addClass('bg-danger');
    //             $('#messageModal').modal('show');
    //         }
    //     });
    // }

    $(document).ready(function () {
        let deleteId;

        window.resetMaterialCode = function (id) {
            deleteId = id;
        };

        $('#confirmDeleteButton').click(function () {

            $('#deleteModal').modal('hide');

            $.ajax({
                url: '@Url.Action("ResetMaterialCode", "EmptyTrayStatusModification")',
                type: 'POST',
                data: {
                    Id: deleteId,
                    __RequestVerificationToken: $('input[name="__RequestVerificationToken"]').val()
                },
                success: function (response) {
                    // 设置消息和类型
                    $('#messageModal .modal-body').text(response.message);
                    $('#messageModal .modal-header').removeClass('bg-success bg-danger')
                        .addClass(response.success ? 'bg-success' : 'bg-danger');

                    // 显示消息模态框
                    $('#messageModal').modal('show');

                    // 设置 1 秒后自动关闭并跳转
                    setTimeout(function () {
                        $('#messageModal').modal('hide'); // 先隐藏提示框
                        window.location.href = '@Url.Action("Index", "EmptyTrayStatusModification")'; // 跳转到首页
                    }, 2000); // 1 秒后

                    // 当用户手动点击关闭按钮或X时也跳转
                    $('#messageModal').on('hidden.bs.modal', function () {
                        window.location.href = '@Url.Action("Index", "EmptyTrayStatusModification")';  // 替换为实际的主页地址
                    });
                },
                error: function () {
                    $('#messageModal .modal-body').text("Delete failed, please try again。");
                    $('#messageModal .modal-header').removeClass('bg-success bg-danger').addClass('bg-danger');
                    $('#messageModal').modal('show');
                }
            });
        });
    });

</script>



